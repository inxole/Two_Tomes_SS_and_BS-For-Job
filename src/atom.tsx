import { atom } from "recoil"

export const Text_Switch = atom({ key: 'TS', default: false })
export const Long_Text = atom({ key: 'LT', default: "page_1" })
export const Pages_Number = atom({ key: 'PN', default: 50 })